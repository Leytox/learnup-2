export default function Loading() {
  return (
    <main className={"min-h-screen flex items-center justify-center"}>
      <div
        className={"size-16 border border-gray-400 rounded-lg animate-spin"}
      />
    </main>
  );
}
